# Bias-Detector-Extension-
Built a Chrome extension with a Java Spring Boot service that evaluates article bias and a fake-news percentage from webpages, pasted URLs, or text/PDFs. Uses JSoup for extraction and Google Gemini API for scoring, with optional on-page highlights.
